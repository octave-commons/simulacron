# OpenCode Session Provenance

This repo is backed more by design recovery than by a single extracted code module.

## Relevant session trail

### `session-ses_3d56.md`
This session artifact records a note bundle that included:
- `promethean-ontology-8-layers.md`
- `cephalon-daimoi-v01.md`
- `cephalon-nexus-index-v01.md`

That matters because `simulacron` is downstream of the same layered ontology work that also produced the daimoi and nexus-index notes.

### `session-ses_3df5.md`
This session lists the recovered note stack and its next steps. It is useful as evidence that the decomposition effort was already trying to separate:
- storage
- retrieval
- daimoi walkers
- nexus indexing

`simulacron` belongs to the same decomposition wave, but on the cognition/entity side rather than the retrieval/runtime side.

## Practical reading path
1. `docs/layers.md`
2. `docs/FORK_TALES_SOURCE_MAP.md`
3. `specs/eta-mu-extraction-vault.md` in the devel workspace
4. `orgs/octave-commons/fork_tales` panel and presence artifacts
5. the Cephalon note bundle listed above

## Caveat
The session-search CLI in `packages/reconstituter` is currently broken in this workspace by an `openplanner-cljs-client` export mismatch, so the provenance recorded here comes from direct searches over the checked-in session artifacts.
